<%= form_with(model: move_effect, local: true) do |form| %>
  <% if move_effect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(move_effect.errors.count, "error") %> prohibited this move_effect from being saved:</h2>

      <ul>
        <% move_effect.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
